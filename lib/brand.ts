/**
 * App branding — single source of truth.
 * Rename the whole app by editing APP_NAME (and MARK if you want a different
 * icon letter). Everything else — sidebar, tab title, PWA manifest — reads this.
 */
// Evolve OS — a system for becoming who you're building toward, one day at a time.
export const APP_NAME = "Evolve OS";
export const APP_TAGLINE = "Become who you're building toward — every single day.";
export const APP_MARK = APP_NAME.charAt(0).toUpperCase();

/** The coach persona's name (shown next to the avatar). */
export const COACH_NAME = "Leo";
